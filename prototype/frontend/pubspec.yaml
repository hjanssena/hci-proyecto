name: bebeia_front
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.1
  flutter_secure_storage: ^10.0.0
  provider: ^6.1.5+1
  jwt_decoder: ^2.0.1
  file_picker: ^10.3.10
  crop_image: ^1.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
